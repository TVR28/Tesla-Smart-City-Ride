configuration = {
    "AWS_ACCESS_KEY": "..",
    "AWS_SECRET_KEY": "..", #You can find these in IAM -> Users -> Security credentials -> Access Keys -> Create a new key and make sure to select "Application Running outside of AWS"
    # Store the Access key and secret Key
}